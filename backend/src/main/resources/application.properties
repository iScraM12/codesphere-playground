# Use the in-memory store by default so the application does not require MongoDB locally.
app.storage=memory

# Switch to MongoDB when a server is available:
# app.storage=mongodb
# quarkus.mongodb.connection-string=mongodb://<host>:27017
quarkus.mongodb.database=codesphere
quarkus.mongodb.devservices.enabled=false
